import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  allowedDevOrigins: [
    'macbookair.home',
    'MacBookair.home',
    'macbookair.local',
    'MacBookair.local',
    'macbookair',
    'MacBookair'
  ]
};

export default nextConfig;
